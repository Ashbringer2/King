# This script stages all changes, commits them with a default message, and pushes to the main branch.

# Stage all changes
git add .

# Commit changes with a default message
git commit -m "Auto commit 2"

# Push changes to the main branch
git push origin main
